<h1 class="center">Enter your course here</h1>
<input class="center" id="filterInput" type="text"/>
<p class="center">Filter by:</p>
<div id="check">
    <input class="prof" type="checkbox">Professor</input>
    <input class="dep" type="checkbox">Department</input>
    <input class="cred" type="checkbox">Credits</input>
</div>
<table id="thisisatable">
    <tr id="row">
        <th>Course</th>
        <th>Department</th>
        <th>Professor</th>
        <th>Credits</th>
    </tr>
    <tr id="row">
        <td class="t_allcourses t_course">Intro to Computer Architecture</td>
        <td class="t_allcourses t_department">Computer Science</td>
        <td class="t_allcourses t_professor">Professor 1</td>
        <td class="t_allcourses t_credits">1</td>
    </tr>
    <tr id="row">
        <td class="t_allcourses t_course">Calculus</td>
        <td class="t_allcourses t_department">Mathematics</td>
        <td class="t_allcourses t_professor">Professor 2</td>
        <td class="t_allcourses t_credits">3</td>
    </tr>
    <tr id="row">
        <td class="t_allcourses t_course">Super Important Class</td>
        <td class="t_allcourses t_department">Super Important Department</td>
        <td class="t_allcourses t_professor">Super Important Professor</td>
        <td class="t_allcourses t_credits">7</td>
    </tr>
    <tr id="row">
        <td class="t_allcourses t_course">Cooking</td>
        <td class="t_allcourses t_department">Cooking Class</td>
        <td class="t_allcourses t_professor">Gordon Ramsay</td>
        <td class="t_allcourses t_credits">5</td>
    </tr>
</table>
<ul class="center" id="courses">
    <li class="course-item"><a>best</a></li>
    <li class="course-item"><a>earn</a></li>
    <li class="course-item"><a>test</a></li>
    <li class="course-item"><a>moon</a></li>
    <li class="course-item"><a>rn</a></li>
</ul>
<!--
<script>
    let filterInput = document.getElementById("filterInput");
    filterInput.addEventListener("keyup", filterCourses); // filterCourses function every time a char is typed
    function filterCourses(){

        let filterValue = document.getElementById("filterInput").value.toUpperCase(); // uppercases typed char
        let ul = document.getElementById("courses");
        let li = ul.querySelectorAll("li.course-item"); // gets all ul items

        for (let i = 0; i < li.length; i++){

            let a = li[i].getElementsByTagName("a")[0];
            if (a.innerHTML.toUpperCase().indexOf(filterValue) > -1){
                li[i].style.display = "";
            } else {
                li[i].style.display = "none"; // no display
            }

        }
    }
</script>
-->
<!-- WIP -->
<script>
    let filterInput = document.getElementById("filterInput");
    filterInput.addEventListener("keyup", filterCourses); // filterCourses function every time a char is typed
    function filterCourses(){

        let filterValue = document.getElementById("filterInput").value.toUpperCase(); // uppercases typed char
        let table = document.getElementById("thisisatable")
        let tr = table.querySelectorAll(".t_allcourses"); // gets all ul items
        console.log("fucntion", tr.length);
        for (let i = 0; i < tr.length; i++){
            console.log("for loop");
            if (tr.toUpperCase().indexOf(filterValue) > -1){
                tr[i].style.display = "";
            } else {
                tr[i].style.display = "none"; // no display
            }
        }
    }
</script>
